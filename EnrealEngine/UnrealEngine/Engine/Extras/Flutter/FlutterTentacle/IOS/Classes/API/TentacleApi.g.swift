// Copyright Epic Games, Inc. All Rights Reserved.
//
// This is an auto-generated file. See FlutterLiveLinkVCAM/pigeons/README for information on how to
// modify the original source and generate a new file.
//
// Autogenerated from Pigeon (v22.7.2), do not edit directly.
// See also: https://pub.dev/packages/pigeon

import Foundation

#if os(iOS)
  import Flutter
#elseif os(macOS)
  import FlutterMacOS
#else
  #error("Unsupported platform.")
#endif

/// Error class for passing custom error details to Dart side.
final class PigeonError: Error {
  let code: String
  let message: String?
  let details: Any?

  init(code: String, message: String?, details: Any?) {
    self.code = code
    self.message = message
    self.details = details
  }

  var localizedDescription: String {
    return
      "PigeonError(code: \(code), message: \(message ?? "<nil>"), details: \(details ?? "<nil>")"
      }
}

private func wrapResult(_ result: Any?) -> [Any?] {
  return [result]
}

private func wrapError(_ error: Any) -> [Any?] {
  if let pigeonError = error as? PigeonError {
    return [
      pigeonError.code,
      pigeonError.message,
      pigeonError.details,
    ]
  }
  if let flutterError = error as? FlutterError {
    return [
      flutterError.code,
      flutterError.message,
      flutterError.details,
    ]
  }
  return [
    "\(error)",
    "\(type(of: error))",
    "Stacktrace: \(Thread.callStackSymbols)",
  ]
}

private func createConnectionError(withChannelName channelName: String) -> PigeonError {
  return PigeonError(code: "channel-error", message: "Unable to establish connection on channel: '\(channelName)'.", details: "")
}

private func isNullish(_ value: Any?) -> Bool {
  return value is NSNull || value == nil
}

private func nilOrValue<T>(_ value: Any?) -> T? {
  if value is NSNull { return nil }
  return value as! T?
}

/// Types of Tentacle products.
enum TentacleProductId: Int {
  case generic = 0
  case syncE = 1
  case trackE = 2
  case timebar = 3
}

/// Information about a Tentacle device. This only includes data which is expected to be requested infrequently, so it
/// excludes the time code, last seen time, etc.
///
/// Generated class from Pigeon that represents data sent in messages.
struct TentacleDeviceInfo {
  /// True if the device is generating timecodes.
  var bIsInGreenMode: Bool
  /// Whether the battery is charging.
  var bIsCharging: Bool
  /// Whether the device is generating drop-frame timecodes.
  var bIsDropFrame: Bool
  /// Current battery level in percent.
  var batteryLevel: Int64
  /// Frame rate of the timecodes generated by this device in frames per second.
  var frameRate: Double
  /// Device timestamp (seconds since boot) when the device was last detected.
  var lastSeenTimestamp: Double
  /// Index of the icon used to represent this device.
  var iconIndex: Int64
  /// Received signal strength in decibel-milliwatts.
  var signalStrength: Int64
  /// The type of Tentacle product.
  var productId: TentacleProductId
  /// Unique identifier string for the device.
  var identifier: String
  /// User-assigned name for the device.
  var name: String


  // swift-format-ignore: AlwaysUseLowerCamelCase
  static func fromList(_ pigeonVar_list: [Any?]) -> TentacleDeviceInfo? {
    let bIsInGreenMode = pigeonVar_list[0] as! Bool
    let bIsCharging = pigeonVar_list[1] as! Bool
    let bIsDropFrame = pigeonVar_list[2] as! Bool
    let batteryLevel = pigeonVar_list[3] as! Int64
    let frameRate = pigeonVar_list[4] as! Double
    let lastSeenTimestamp = pigeonVar_list[5] as! Double
    let iconIndex = pigeonVar_list[6] as! Int64
    let signalStrength = pigeonVar_list[7] as! Int64
    let productId = pigeonVar_list[8] as! TentacleProductId
    let identifier = pigeonVar_list[9] as! String
    let name = pigeonVar_list[10] as! String

    return TentacleDeviceInfo(
      bIsInGreenMode: bIsInGreenMode,
      bIsCharging: bIsCharging,
      bIsDropFrame: bIsDropFrame,
      batteryLevel: batteryLevel,
      frameRate: frameRate,
      lastSeenTimestamp: lastSeenTimestamp,
      iconIndex: iconIndex,
      signalStrength: signalStrength,
      productId: productId,
      identifier: identifier,
      name: name
    )
  }
  func toList() -> [Any?] {
    return [
      bIsInGreenMode,
      bIsCharging,
      bIsDropFrame,
      batteryLevel,
      frameRate,
      lastSeenTimestamp,
      iconIndex,
      signalStrength,
      productId,
      identifier,
      name,
    ]
  }
}

private class TentacleApiPigeonCodecReader: FlutterStandardReader {
  override func readValue(ofType type: UInt8) -> Any? {
    switch type {
    case 129:
      let enumResultAsInt: Int? = nilOrValue(self.readValue() as! Int?)
      if let enumResultAsInt = enumResultAsInt {
        return TentacleProductId(rawValue: enumResultAsInt)
      }
      return nil
    case 130:
      return TentacleDeviceInfo.fromList(self.readValue() as! [Any?])
    default:
      return super.readValue(ofType: type)
    }
  }
}

private class TentacleApiPigeonCodecWriter: FlutterStandardWriter {
  override func writeValue(_ value: Any) {
    if let value = value as? TentacleProductId {
      super.writeByte(129)
      super.writeValue(value.rawValue)
    } else if let value = value as? TentacleDeviceInfo {
      super.writeByte(130)
      super.writeValue(value.toList())
    } else {
      super.writeValue(value)
    }
  }
}

private class TentacleApiPigeonCodecReaderWriter: FlutterStandardReaderWriter {
  override func reader(with data: Data) -> FlutterStandardReader {
    return TentacleApiPigeonCodecReader(data: data)
  }

  override func writer(with data: NSMutableData) -> FlutterStandardWriter {
    return TentacleApiPigeonCodecWriter(data: data)
  }
}

class TentacleApiPigeonCodec: FlutterStandardMessageCodec, @unchecked Sendable {
  static let shared = TentacleApiPigeonCodec(readerWriter: TentacleApiPigeonCodecReaderWriter())
}

/// API that receives messages about Tentacle devices in the host language.
///
/// Generated protocol from Pigeon that represents a handler of messages from Flutter.
protocol TentacleHostApi {
  /// Start scanning for Tentacle devices using BLE.
  /// Bluetooth permission must be acquired before calling this.
  func startScanning() throws
  /// Stop scanning for Tentacle devices using BLE.
  func stopScanning() throws
  /// Get the latest list of cached device information.
  func getCachedDeviceInfo() throws -> [TentacleDeviceInfo]
}

/// Generated setup class from Pigeon to handle messages through the `binaryMessenger`.
class TentacleHostApiSetup {
  static var codec: FlutterStandardMessageCodec { TentacleApiPigeonCodec.shared }
  /// Sets up an instance of `TentacleHostApi` to handle messages through the `binaryMessenger`.
  static func setUp(binaryMessenger: FlutterBinaryMessenger, api: TentacleHostApi?, messageChannelSuffix: String = "") {
    let channelSuffix = messageChannelSuffix.count > 0 ? ".\(messageChannelSuffix)" : ""
    /// Start scanning for Tentacle devices using BLE.
    /// Bluetooth permission must be acquired before calling this.
    let startScanningChannel = FlutterBasicMessageChannel(name: "dev.flutter.pigeon.flutter_tentacle.TentacleHostApi.startScanning\(channelSuffix)", binaryMessenger: binaryMessenger, codec: codec)
    if let api = api {
      startScanningChannel.setMessageHandler { _, reply in
        do {
          try api.startScanning()
          reply(wrapResult(nil))
        } catch {
          reply(wrapError(error))
        }
      }
    } else {
      startScanningChannel.setMessageHandler(nil)
    }
    /// Stop scanning for Tentacle devices using BLE.
    let stopScanningChannel = FlutterBasicMessageChannel(name: "dev.flutter.pigeon.flutter_tentacle.TentacleHostApi.stopScanning\(channelSuffix)", binaryMessenger: binaryMessenger, codec: codec)
    if let api = api {
      stopScanningChannel.setMessageHandler { _, reply in
        do {
          try api.stopScanning()
          reply(wrapResult(nil))
        } catch {
          reply(wrapError(error))
        }
      }
    } else {
      stopScanningChannel.setMessageHandler(nil)
    }
    /// Get the latest list of cached device information.
    let getCachedDeviceInfoChannel = FlutterBasicMessageChannel(name: "dev.flutter.pigeon.flutter_tentacle.TentacleHostApi.getCachedDeviceInfo\(channelSuffix)", binaryMessenger: binaryMessenger, codec: codec)
    if let api = api {
      getCachedDeviceInfoChannel.setMessageHandler { _, reply in
        do {
          let result = try api.getCachedDeviceInfo()
          reply(wrapResult(result))
        } catch {
          reply(wrapError(error))
        }
      }
    } else {
      getCachedDeviceInfoChannel.setMessageHandler(nil)
    }
  }
}
/// API that receives messages about Tentacle devices in Flutter.
///
/// Generated protocol from Pigeon that represents Flutter messages that can be called from Swift.
protocol TentacleFlutterApiProtocol {
}
class TentacleFlutterApi: TentacleFlutterApiProtocol {
  private let binaryMessenger: FlutterBinaryMessenger
  private let messageChannelSuffix: String
  init(binaryMessenger: FlutterBinaryMessenger, messageChannelSuffix: String = "") {
    self.binaryMessenger = binaryMessenger
    self.messageChannelSuffix = messageChannelSuffix.count > 0 ? ".\(messageChannelSuffix)" : ""
  }
  var codec: TentacleApiPigeonCodec {
    return TentacleApiPigeonCodec.shared
  }
}
